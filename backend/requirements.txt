# For Embedding Models
# NOTE: Embeddings are computed via the HuggingFace Inference API (see
# utils/embeddings.py), NOT a local model. torch/sentence-transformers/
# transformers are intentionally NOT installed because they exceed Render's
# 512 MB free tier and OOM-kill the container during uploads.
numpy==1.26.4

# For Text Chunking
langchain==0.1.15

# For Document Parsing
PyMuPDF==1.23.21      # (fitz)
python-docx==1.1.0

# For Text Processing
nltk==3.8.1

# MongoDB Driver (optional, for later DB interactions)
pymongo==4.5.0

# For API calls
requests==2.31.0

# For environment variables
python-dotenv==1.0.1
psutil==5.9.8

# For Backend API
flask==2.3.3
flask-cors==4.0.0
waitress==3.0.0

# For Translation
langdetect==1.0.9