# For Embedding Models
# NOTE: torch is installed as the CPU-only build in the Dockerfile to avoid
# pulling the multi-GB CUDA stack (cudnn/cublas/etc.), which OOM-kills the
# 512 MB free tier. torchvision/torchaudio are unused and intentionally removed.
sentence-transformers==5.1.0
huggingface-hub==0.34.4
torch==2.9.0
tokenizers==0.20.0
transformers==4.45.0
numpy==1.26.4

# For Text Chunking
langchain==0.1.15

# For Document Parsing
PyMuPDF==1.23.21      # (fitz)
python-docx==1.1.0

# For Text Processing
nltk==3.8.1

# MongoDB Driver (optional, for later DB interactions)
pymongo==4.5.0

# For API calls
requests==2.31.0

# For environment variables
python-dotenv==1.0.1
psutil==5.9.8

# For Backend API
flask==2.3.3
flask-cors==4.0.0
waitress==3.0.0

# For Translation
langdetect==1.0.9